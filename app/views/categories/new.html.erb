<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Nouvelle catégorie</h1>

  <%= render "form", category: @category, path: list_categories_path(@list) %>

  <%= link_to 'Retour aux listes', list_path(@list), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
